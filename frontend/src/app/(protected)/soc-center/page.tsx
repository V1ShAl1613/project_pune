"use client";

// SOC center dashboard route that anchors the security operations workspace.
import { SocDashboardPage } from "@/soc/shared/soc-dashboard-page";

export default function SocCenterPage(): React.JSX.Element {
  return <SocDashboardPage />;
}
